/**
 * @module core/scheduler/index.js
 * @description Scheduler global API exposure.
 */

// Section: SCHEDULER MODULE INDEX

// Preserve existing global API contract.
window.renderMultiClasses = renderMultiClasses;
