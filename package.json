{
  "name": "timetable-generator",
  "version": "1.0.0",
  "description": "Customizable college timetable generator with constraint-based scheduling",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "test": "jest --verbose",
    "lint": "eslint src/js/ --ext .js",
    "format": "prettier --write src/js/ src/css/"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "eslint": "^8.57.0",
    "fast-check": "^4.6.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "prettier": "^3.2.0"
  }
}
